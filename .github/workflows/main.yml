# main.yml
name: General CI

on:
  push:
    branches-ignore: ["main"]  # Ignore pushes to main branch to avoid duplicate deployment
  pull_request:
    branches:
      - main

jobs:
  # Define other CI jobs (e.g., tests or linter) without deployment
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Add any additional testing or linting steps here
